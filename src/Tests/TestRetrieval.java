package Tests;

import Step3.Retrieval;

import java.util.HashMap;
import java.util.Map;

public class TestRetrieval {
    public static void main(String[] args) {
        Retrieval retrieval = new Retrieval();
        /*
        retrieval.run("BBC World Service staff cuts");

        HashMap<String, Double> scores = retrieval.getScores();

        int counter = 0;
        for (Map.Entry<String, Double> each : scores.entrySet()) {
            counter += 1;
            if (counter <= 100) {
                System.out.println(each.getValue());
            } else {
                break;
            }
        }

         */
    }
}
